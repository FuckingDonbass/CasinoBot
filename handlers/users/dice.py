from aiogram import types
from states import Func, State
from loader import dp
import asyncio

#
# @dp.message_handler(emoji=["🎲"])
# async def dice(message: types.Message):
#     await message.answer("ку")
